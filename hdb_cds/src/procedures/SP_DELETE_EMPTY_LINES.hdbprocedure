PROCEDURE "SP_DELETE_EMPTY_LINES"(
	-- IN IN_INT INTEGER
	IN FILENAME STRING
)
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   --DEFAULT SCHEMA <default_schema_name>
   AS
BEGIN
	delete from "CongressMarks.LINES" where "FILENAME" = FILENAME AND ltrim(rtrim("TEXT")) = '';
END