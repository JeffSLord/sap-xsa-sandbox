PROCEDURE "sap_xsa_sandbox.hdb_cds.procedures::SP_DELETE_PAGE"(
	IN FILENAME STRING
)
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   --DEFAULT SCHEMA <default_schema_name>
   AS
BEGIN
   DELETE FROM "sap_xsa_sandbox.hdb_cds::CongressMarks.PAGES" WHERE "FILENAME" = FILENAME;
END