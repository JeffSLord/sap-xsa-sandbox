PROCEDURE "SP_DELETE_PAGE"(
	IN FILENAME STRING
)
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   --DEFAULT SCHEMA <default_schema_name>
   AS
BEGIN
   DELETE FROM "CongressMarks.PAGES" WHERE "FILENAME" = FILENAME;
END